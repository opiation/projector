/**
 * This prettier configuration is intentionally empty to reduce the incentive
 * to care about anything format-related.
 *
 * @type {import("prettier").Config}
 */
const config = {};

export default config;
